var val =0; // Variables are defined like this
SetFunction Start {} // This is a comment
    val += 1; // Supports syntax sugar
    log(val);
    val = val + 1
    log(val);
EndFunction

/*
    This is a multiline comment
*/

SetFunction OnBeat {beat}
    log("Beat: " + beat);
EndFunction