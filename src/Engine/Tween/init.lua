-- All of the "Tween" code is based of HaxeFlixel's FlxTween class.

local path = ... .. "."

TweenType = require(path .. "TweenType")

Ease = require(path .. "Ease")
TweenManager = require(path .. "TweenManager")
Tween = require(path .. "Tween")
VarTween = require(path .. "VarTween")
